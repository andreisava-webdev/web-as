import { extendTheme } from '@chakra-ui/react';

const colors = {
  brand: {
    primary: '#109CF1',
    gray: '#90A0B7',
    lightGray: 'C2CFE0',
  },
};

const theme = extendTheme({ colors });

export default theme;
